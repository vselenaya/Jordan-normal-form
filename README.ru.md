# Вычисление Жордановой нормальной формы (ЖНФ)

Данный репозиторий содержит реализацию алгоритма построения **Жордановой нормальной формы** (Jordan Normal Form, JNF) матрицы и соответствующего **базиса Жордана** (матрицы перехода).

Проект предлагает два варианта реализации: для вещественных чисел (с подробными комментариями) и для конечных полей (точная арифметика).

## 📂 Структура репозитория

В проекте представлены два основных файла:

### 1. `with-comments.cpp` (Базовая версия)

* **Тип данных:** `long double` (вещественные числа).
* **Особенности:**
* Содержит подробные теоретические вставки и комментарии к каждому шагу алгоритма.
* Идеально подходит для изучения алгоритма и понимания работы корневых подпространств и цепочек.
* Использует `EPSILON` для компенсации погрешностей плавающей точки.



### 2. `with-check-and-module.cpp` (Расширенная версия)

* **Тип данных:** Работает в том числе над полем вычетов (по простому модулю, его можно менять в начале файла).
* **Особенности:**
* Точная арифметика: Полностью исключает ошибки округления, свойственные `float/double`. Операции (деление, умножение) выполняются через модульную инверсию.
* Автоматическая проверка: Включает блок верификации результата. Программа перемножает полученные матрицы, чтобы убедиться в верности равенства.



## 🚀 Использование

Для работы алгоритма требуются входные данные двух типов:

1. **Квадратная матрица** размера $n \times n$.
2. **Спектр матрицы** (список собственных чисел), включая их алгебраическую кратность.

> **Примечание:** Алгоритм фокусируется на линейной алгебре (построение цепочек), поэтому поиск корней характеристического многочлена (собственных чисел) должен быть выполнен заранее.

### Пример входных данных

Рассмотрим матрицу:
$$
A = \begin{pmatrix}
3 & 1 & -4 & -7 \\
-1 & 1 & 5 & 9 \\
0 & 0 & 4 & 4 \\
0 & 0 & -1 & 0
\end{pmatrix}
$$

Характеристический многочлен этой матрицы: $(\lambda-2)^4.
Следовательно, собственное число $\lambda=2$  имеет алгебраическую кратность 4.

**Ввод в программу:**

```text
4
3 1 -4 -7
-1 1 5 9
0 0 4 4
0 0 -1 0
2 2 2 2
```

### Сборка и запуск

Для компиляции используйте `g++`:

```bash
# Для версии с комментариями
g++ with-comments.cpp -o jnf_float
./jnf_float

# Для версии с модульной арифметикой
g++ with-check-and-module.cpp -o jnf_mod
./jnf_mod
```
